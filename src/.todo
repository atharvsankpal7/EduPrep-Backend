use helmet library to add various HTTP headers for security
add persistent state management

when the user uploads the excel sheet in question.controller.ts for getTopicIds function update the functionality to accept the optional parameter from the user in the api to add the topic for the given subject.
currently the api only handles the topics that are already present in the database for given subject.